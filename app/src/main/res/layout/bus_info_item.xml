<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?android:attr/selectableItemBackground"
    android:padding="10dp">

    <ImageView
        android:id="@+id/infoIcon"
        android:layout_width="@dimen/companyInfoIconSize"
        android:layout_height="@dimen/companyInfoIconSize"
        android:layout_gravity="center_vertical"
        tools:ignore="ContentDescription"
        tools:src="@drawable/ic_mail_outline_black_24dp" />

    <TextView
        android:id="@+id/infoKey"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:layout_margin="5dp"
        android:layout_marginEnd="5dp"
        android:layout_marginStart="5dp"
        android:maxLines="1"
        android:textSize="16sp"
        tools:text="Adres email:" />

    <TextView
        android:id="@+id/infoValue"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center_vertical"
        android:layout_margin="5dp"
        android:gravity="end"
        android:maxLines="1"
        android:textSize="16sp"
        tools:text="exampleil.com" />


</LinearLayout>
